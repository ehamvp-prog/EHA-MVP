import { InstallerSetup } from "@/components/installer-setup"

export const metadata = {
  title: "Installer Setup · Elevate Home App",
  description: "One-time system profile for this home.",
}

export default function SetupPage() {
  return <InstallerSetup />
}
